# Optimal-split-of-orders-across-liquidity-pools
We implement the stochastic approximation algorithm to compute the optimal allocation of buy orders across dark pools when the total available liquidity is insufficient to fill the entire order, i.e., in a shortage scenario.
